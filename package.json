{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "gen": "turbo gen",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky",
    "web:migrate": "pnpm --filter web run migrate",
    "formui:generate:component": "pnpm --filter @repo/form-ui run generate:component"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.0",
    "prettier-plugin-tailwindcss": "^0.6.4",
    "turbo": "^2.0.4",
    "@turbo/gen": "^2.0.4"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=20"
  },
  "name": "compr"
}
